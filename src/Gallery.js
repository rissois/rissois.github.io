import { Box, CardMedia, Card, Typography, Container } from '@mui/material';


function Gallery({ name, description, photos, styles }) {

  return (
    <Container>

    </Container>
  );
}

export default Gallery;
